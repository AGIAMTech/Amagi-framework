# YouTube Shorts generator — Python dependencies
# Install: pip install -r requirements.txt

edge-tts>=7.2.0          # Microsoft Neural TTS (free, no API key)
Pillow>=10.0.0           # Image generation (branded frame 1080x1920)

# YouTube upload (optional — only needed if YOUTUBE_REFRESH_TOKEN is set)
google-api-python-client>=2.100.0
google-auth>=2.25.0
google-auth-oauthlib>=1.2.0
